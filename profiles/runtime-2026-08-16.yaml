schemaVersion: 4
id: salida-cyl-nan-opencode-analysis-2026-08-16
bindings:
  orchestrator:
    harness: codex
    provider: openai
    model: gpt-5.6-sol
    authentication: chatgpt-subscription
    capability: planning
    allowedDataScopes: [SOURCE_CODE_ONLY]
    allowedSourceSensitivity: [PUBLIC, PRIVATE]
    permissions: read-only
    guidance: &frontier_guidance
      id: openai-sol-current
      revision: upstream-v0.3.1-2026-08-16
      sourceUrls: [https://developers.openai.com/api/docs/guides/latest-model]
      promptFormat: markdown
      contextPlacement: before-task
      reasoningEffort: high
      textVerbosity: low
      temperature: null
      maxSteps: 64
      instructions:
        - Keep prompts lean and preserve explicit constraints, evidence requirements, and success criteria.
  executor:
    harness: opencode
    provider: nan
    model: qwen3.6
    authentication: provider-api-key
    capability: coding
    allowedDataScopes: [SOURCE_CODE_ONLY]
    allowedSourceSensitivity: [PUBLIC]
    permissions: contract-write
    guidance:
      id: qwen3p6-nan
      revision: nan-api-2026-08-16
      sourceUrls:
        [https://nan.builders/docs/api, https://nan.builders/docs/models]
      promptFormat: markdown
      contextPlacement: before-task
      reasoningEffort: provider-default
      textVerbosity: low
      temperature: null
      maxSteps: 32
      instructions:
        - Use direct tool calls, change only contract-authorized paths, and stop after deterministic validation.
  escalationExecutor:
    harness: opencode
    provider: nan
    model: deepseek-v4-flash
    authentication: provider-api-key
    capability: coding
    allowedDataScopes: [SOURCE_CODE_ONLY]
    allowedSourceSensitivity: [PUBLIC]
    permissions: contract-write
    guidance: &deepseek_guidance
      id: deepseek-v4-flash-nan
      revision: nan-api-2026-08-16
      sourceUrls:
        [https://nan.builders/docs/api, https://nan.builders/docs/models]
      promptFormat: markdown
      contextPlacement: before-task
      reasoningEffort: provider-default
      textVerbosity: low
      temperature: null
      maxSteps: 32
      instructions:
        - Use native tool calls, follow the bounded repair contract, and make only the requested changes.
  frontierExecutor:
    harness: opencode
    provider: nan
    model: deepseek-v4-flash
    authentication: provider-api-key
    capability: coding
    allowedDataScopes: [SOURCE_CODE_ONLY]
    allowedSourceSensitivity: [PUBLIC]
    permissions: contract-write
    guidance: *deepseek_guidance
  reviewer:
    harness: codex
    provider: openai
    model: gpt-5.6-sol
    authentication: chatgpt-subscription
    capability: review
    allowedDataScopes: [SOURCE_CODE_ONLY]
    allowedSourceSensitivity: [PUBLIC, PRIVATE]
    permissions: read-only
    guidance: *frontier_guidance
runtime:
  maxEconomyParallelRequests: 1
  maxConcurrentRunsPerRepository: 1
