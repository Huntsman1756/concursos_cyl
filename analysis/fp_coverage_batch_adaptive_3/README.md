# Adaptive FP occupation-coverage batch 3

This wave skips the already researched unresolved programs `SAN06S`, `COM01S`
and `SAN01M`, then covers queue ranks 4–11: `ELE02S`, `EOC01S`, `IMP01S`,
`HOT02M`, `MAM01B`, `COM03S`, `SAN32` and `INA01M`.

Each source file transcribes the official TodoFP professional outputs retained
in `data/curated/professional-profiles.json`. NAN generates review candidates
against the curated CNO-11 catalog; Frontier independently accepts or rejects
every relationship before any public-data change.
