# FP Coverage Wave 2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish twelve conservative modality-expanded FP-to-CNO rows for the next five queue bases and regenerate a reproducible coverage freeze.

**Architecture:** Record primary evidence and Frontier decisions first, add only seven required CNO records and the exact approved keys, then regenerate queue, snapshot, restore boundary, freeze, and contest documents.

**Tech Stack:** JSON, TypeScript, Zod, Vitest, Node.js 24.

**Spec:** `docs/superpowers/specs/2026-08-21-parallel-continuation-wave-design.md`

## Global Constraints

- Use only TodoFP, BOE CNO-11, and INE notes as publication evidence.
- Publish exactly the twelve approved keys in the spec and no aliases.
- Keep ambiguous candidates rejected in the Frontier record.
- Never mutate snapshot `20260821162954121-087e3c5155c6`.
- Prefix repository shell commands with `rtk`.

---

### Task 1: Freeze evidence and red expectations

**Files:**
- Create: `analysis/fp_coverage_priority_20260821_wave2/README.md`
- Create: `analysis/fp_coverage_priority_20260821_wave2/batch-contract.json`
- Create: five source notes and five proposal files below that directory
- Create: `analysis/fp_coverage_priority_20260821_wave2/frontier-review.md`
- Modify: `scripts/data/validateCuratedMappings.test.ts`

**Interfaces:**
- Produces the exact accepted key set and rejected-alternative record consumed by curated publication.

- [ ] **Step 1: Write evidence artifacts**

Record exact TodoFP output quotes, official URLs, CNO rationale, rejected alternatives, seven new occupation codes, and the twelve accepted keys from the spec. Mark `SAN02S|3316` as direct official output and the other rows as reviewed relationships unless existing mapping policy requires the same stronger type.

- [ ] **Step 2: Add exact failing mapping expectations**

Assert that the seven occupation codes exist, the twelve relation keys exist, no wave alias exists, and totals equal 240 relations, 91 base qualifications, and 109 modality keys.

- [ ] **Step 3: Run red**

Run: `rtk npm test -- scripts/data/validateCuratedMappings.test.ts`

Expected: FAIL on missing occupations/relations and old totals.

- [ ] **Step 4: Commit evidence and red tests**

```bash
rtk git add analysis/fp_coverage_priority_20260821_wave2 scripts/data/validateCuratedMappings.test.ts
rtk git commit -m "test(data): define second priority FP wave"
```

### Task 2: Publish curated rows and restore boundary

**Files:**
- Modify: `data/curated/occupations.json`
- Modify: `data/curated/training-occupation-links.json`
- Modify: `scripts/data/restoreFrontierReviewedCoverage.ts`
- Modify: `scripts/data/restoreFrontierReviewedCoverage.test.ts`

**Interfaces:**
- Produces seven approved occupation entries and exactly twelve approved relation rows.
- Extends `ACCEPTED_RELATION_KEYS` with the exact spec keys.

- [ ] **Step 1: Add the seven curated occupations**

Copy preferred labels and official classification identity from `official-occupations.json`; add approved review metadata consistent with neighboring entries.

- [ ] **Step 2: Add the twelve relation rows**

Use the reviewed TodoFP quote for each program/modality, official URLs, `reviewedAt: "2026-08-21"`, and existing mapping version. Do not add generic or rejected codes.

- [ ] **Step 3: Extend restore and tests**

Append the twelve keys to `ACCEPTED_RELATION_KEYS`; assert the wave is restored from a complete reviewed source and cannot be invented when absent.

- [ ] **Step 4: Run focused green tests**

Run: `rtk npm test -- scripts/data/validateCuratedMappings.test.ts scripts/data/restoreFrontierReviewedCoverage.test.ts scripts/data/buildDerivedFpOccupationGraph.test.ts`

- [ ] **Step 5: Commit curated publication**

```bash
rtk git add data/curated/occupations.json data/curated/training-occupation-links.json scripts/data/restoreFrontierReviewedCoverage.ts scripts/data/restoreFrontierReviewedCoverage.test.ts scripts/data/validateCuratedMappings.test.ts
rtk git commit -m "feat(data): publish second priority FP wave"
```

### Task 3: Regenerate queue, snapshot, freeze, and contest documents

**Files:**
- Modify generated analysis queue/outcomes and contest matrix artifacts
- Create a new immutable directory below `public/data/v1/snapshots/`
- Modify: `public/data/v1/manifest.json`
- Modify: `scripts/data/restoreFrontierReviewedCoverage.ts`
- Modify: `scripts/data/restoreFrontierReviewedCoverage.test.ts`
- Modify: `scripts/release/validateContestFreeze.test.ts`
- Modify generated files below `docs/contest/`

**Interfaces:**
- Produces 240 approved rows, 91 reviewed bases, 109 modalities, 47 pending bases, 16 no-match bases, and 21 aliases.

- [ ] **Step 1: Regenerate the research queue**

Run: `rtk npm run analysis:fp:research-queue`.

Verify the five wave bases disappear and pending count is 47.

- [ ] **Step 2: Build the immutable snapshot**

Run: `rtk npm run data:build`.

Verify the old snapshot bytes are unchanged and the manifest addresses one new snapshot.

- [ ] **Step 3: Pin the restore source boundary**

Commit the curated/generated data, capture that commit SHA, update the restore source constant and its exact test, then commit the pin separately.

- [ ] **Step 4: Regenerate freeze and rendered documents**

Run: `rtk npx tsx scripts/release/validateContestFreeze.ts --write`.

Run: `rtk npx tsx scripts/release/renderContestSubmission.ts --write`.

Update exact fixture counts only from generated output.

- [ ] **Step 5: Run the complete focused boundary**

Run: `rtk npm test -- scripts/data/validateCuratedMappings.test.ts scripts/data/restoreFrontierReviewedCoverage.test.ts scripts/data/buildSnapshots.test.ts scripts/data/validatePublicDistribution.test.ts scripts/analysis/buildFpCoverageResearchQueue.test.ts scripts/analysis/buildContestEvidenceMatrix.test.ts scripts/release/validateContestFreeze.test.ts scripts/release/renderContestSubmission.test.ts`

Run: `rtk npm run contest:submission:check`.

- [ ] **Step 6: Commit generated boundary**

Stage only reviewed generated data, analysis, restore fixtures, freeze, and rendered contest documents; commit with `feat(data): publish second priority FP snapshot`.
