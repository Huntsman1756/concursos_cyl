/**
 * Compatibility export for consumers that still import the former route
 * component. The canonical center catalog now lives in the centers feature.
 */
export { CentersExplorerPage as TrainingRoutePage } from "../centers/CentersExplorerPage";
